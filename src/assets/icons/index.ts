/**
 * Icon Components Export
 * 
 * This folder is for SVG icons imported as React components.
 * 
 * Usage:
 * 1. Add your SVG file (e.g., MyIcon.tsx)
 * 2. Export it from this index file
 * 3. Import in your component: import { MyIcon } from '@/assets/icons'
 * 
 * Example icon component (MyIcon.tsx):
 * 
 * export function MyIcon({ className = '', size = 24 }: { className?: string; size?: number }) {
 *   return (
 *     <svg
 *       width={size}
 *       height={size}
 *       viewBox="0 0 24 24"
 *       fill="none"
 *       className={className}
 *     >
 *       <path d="..." fill="currentColor" />
 *     </svg>
 *   );
 * }
 */

// Export your icon components here
// export { MyIcon } from './MyIcon';
